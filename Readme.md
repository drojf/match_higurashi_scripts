```
usage: match_higurashi_sprites.py [-h]
error: the following arguments are required: steam_scripts_folder, ps3_scripts_folder, output_file_path
                                  steam_scripts_folder ps3_scripts_folder
                                  output_file_path

Match sprites between steam and ps3 scripts, given two input folders
containing scripts as .txt files.

positional arguments:
  steam_scripts_folder  path containing steam scripts as .txt files
  ps3_scripts_folder    path containing ps3 scripts as .txt files
  output_file_path      name of output file where results are written

optional arguments:
  -h, --help            show this help message and exit
```